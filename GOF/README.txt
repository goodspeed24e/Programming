Note that DPC++.zip and DPC++.tar.Z are the same as DPCPP.zip and 
DPCPP.tar.Z respectively.  The only reason the seperate files exist 
are for people with systems whose file system cannot represent the
plus (+) character in a filename.

Same goes for gmk++.tar.gz.

